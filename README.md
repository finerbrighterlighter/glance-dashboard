# Glance Dashboard

Personal Glance dashboard configuration.

## Setup

1. Copy `.env.example` → `.env`
2. Fill in API keys
3. Run:

```bash
docker compose up -d
```
### Notes
- .env is intentionally not tracked
- Assets and config are versioned